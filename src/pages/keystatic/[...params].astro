---
// src/pages/keystatic/[...params].astro
export const prerender = true;

export function getStaticPaths() {
  return [
    { params: { params: undefined } } // This covers the root /keystatic path
  ];
}

import { KeystaticApp } from './_app';
---

<KeystaticApp client:only="react" />
